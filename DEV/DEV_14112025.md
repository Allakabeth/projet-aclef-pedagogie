# Journal de développement - 14/11/2025

## [2025-11-15 04:00] - 🎉 MERGE PRODUCTION: Refonte Reconnaître les mots

### 🚀 DÉPLOIEMENT RÉUSSI
La branche `refonte-reconnaitre-mots` a été **mergée dans `main`** et **déployée sur Vercel**.

### Production
✅ **URL:** https://projet-aclef-pedagogie.vercel.app/lire/reconnaitre-les-mots

### Impact
- **Ancien code supprimé:** -5,579 lignes (fichier monolithique)
- **Nouveau code ajouté:** +11,177 lignes (architecture modulaire)
- **39 fichiers modifiés**
- **Aucun conflit non résolu**
- **Tests validés en local et production**

### Architecture déployée
- ✅ Exercices séparés (9 exercices indépendants)
- ✅ UI cohérente turquoise/jaune
- ✅ Système génération phrases lazy (cache-first)
- ✅ Migration SQL phrases pré-générées
- ✅ Journaux DEV complets

### Commits principaux
- `6189b54` - Merge refonte dans main
- `a322ae1` - Mise à jour journal DEV
- `42832e0` - Simplification système phrases

### Sauvegardes
- `backup-main-avant-merge` - État main pré-merge
- `backup-refonte-avant-merge` - État refonte pré-merge

---

## [2025-11-15 03:30] - Refactor: Simplification système génération phrases

### Fichier(s) modifié(s)
- `pages/api/phrases/generer.js` - Refonte complète : lazy loading + Groq uniquement
- `pages/api/textes/creer.js` - Suppression hook auto-génération
- `clean-groq-cache.js` - Script nettoyage cache (créé)
- `pages/api/admin/clean-groq-cache.js` - API nettoyage cache (créée)
- `pages/admin/lire/phrases-pregenerees.js` - Page monitoring (créée)

### Modifications
- **Architecture simplifiée** : Abandon pré-génération massive → génération à la demande
- **Cache-first pattern** : BDD comme cache, génération uniquement si nécessaire
- **Groq uniquement** : Gemini retiré pour cet exercice (3 clés testées, toutes en erreur 404)
- **Messages explicites** : "Plus de crédits disponibles actuellement pour générer de nouvelles phrases" au lieu de "Erreur serveur"
- **Nettoyage cache** : 68 phrases Groq supprimées (en plus des 290 précédentes)
- Hook auto-génération supprimé de la création de texte

### Code important
```javascript
// Génération à la demande avec cache
// 1. Check cache first
const { data: phrasesExistantes } = await supabaseAdmin
    .from('phrases_pregenerees')
    .select('*')
    .eq('user_id', userId)
    .contains('texte_ids', texteIdsNormalises)

// 2. Si cache hit → return immédiat
if (phrasesExistantes?.length > 0) {
    return shuffledPhrases
}

// 3. Si cache miss → générer + stocker + return
const { phrases } = await genererPhrasesIA(motsUniques) // Groq uniquement
await supabaseAdmin.from('phrases_pregenerees').insert(phrasesAInserer)
```

### Commit
- SHA: `42832e08e2716313b1ea541fc3c417c0f41f70da`
- Message: "Refactor: Simplification système génération phrases"
- Lignes: +1054 -286

### Statut
✅ Push réussi sur branche `refonte-reconnaitre-mots`
⏳ En attente recharge quotas Groq pour test complet

---

## [2025-11-15 03:00] - 💾 SAUVEGARDE: Simplification système génération phrases + fix Gemini

⚠️ **Sauvegarde avant compactage de conversation**

### Contexte
Conversation proche de la limite de tokens - sauvegarde des modifications non pushées.
Refonte complète du système de génération de phrases pour "Construis phrases".

### Fichiers modifiés
- `/pages/api/phrases/generer.js` - **REFONTE COMPLÈTE** : génération à la demande (lazy loading) au lieu de pré-génération massive
- `/pages/admin/lire/phrases-pregenerees.js` - Retrait bouton génération massive
- `/pages/api/textes/creer.js` - Suppression hook auto-génération
- `/pages/api/admin/clean-groq-cache.js` - API nettoyage cache (créée)
- `clean-groq-cache.js` - Script temporaire (à supprimer)

### Fichiers SUPPRIMÉS (6 fichiers)
- `/api/phrases/generer-toutes-combinaisons.js` - Génération massive obsolète
- `/api/admin/phrases/generer-tous-apprenants.js` - Bulk generation obsolète
- `/api/admin/phrases/debug-apprenants.js` - Debug API
- `/api/admin/phrases/diagnostic-textes.js` - Debug API
- `/api/admin/phrases/test-generation.js` - Test API
- `/pages/admin/lire/test-generation-debug.js` - Test page

### Travail terminé
- ✅ Refactorisation complète `/api/phrases/generer.js` vers génération lazy (cache-first)
- ✅ Suppression 6 fichiers de génération massive
- ✅ Retrait bouton "Générer toutes les phrases" page admin
- ✅ Suppression hook auto-génération dans création de texte
- ✅ Nettoyage 290 phrases Groq invalides du cache ("Je voir ça si ça. Alice du paté.")
- ✅ Migration SDK Gemini (au lieu d'API REST qui échouait)
- ✅ Retrait fallback Groq (sur demande utilisateur explicite)

### Architecture nouvelle (IMPORTANTE)
**Avant (système complexe - SUPPRIMÉ) :**
- Pré-génération de TOUTES les combinaisons possibles de textes
- Génération en arrière-plan lors de la création de texte
- Scripts admin de génération massive
- Rate limiting, échecs, phrases invalides

**Après (système simple - ACTUEL) :**
```javascript
// /api/phrases/generer.js - Handler principal
export default async function handler(req, res) {
    // 1. Auth JWT
    // 2. Normaliser texte_ids (tri pour cache matching)
    // 3. Chercher en cache (table phrases_pregenerees)
    // 4. Si trouvé → return immédiat (10 phrases aléatoires)
    // 5. Si NON trouvé → générer avec Gemini + stocker + return
}
```

**Flow utilisateur :**
1. Apprenant sélectionne texte(s) → exercice "Construis phrases"
2. Frontend → POST `/api/phrases/generer` avec `texte_ids: [1,2,3]`
3. API vérifie cache → HIT: 100-200ms / MISS: 10-30s (génération IA)
4. Frontend affiche phrases

**Table BDD conservée (cache essentiel) :**
- `phrases_pregenerees` - Stocke phrases générées par IA
- Index GIN sur `texte_ids` pour recherche rapide
- Contrainte unicité `(texte_ids, phrase, user_id)`

### Problèmes rencontrés et solutions

**1. Système de pré-génération massive échouait**
- 14 apprenants traités, seulement 2 avec phrases générées
- Cause : rate limiting Gemini/Groq lors d'appels simultanés
- **Solution :** Abandon complet du système de pré-génération

**2. Groq générait phrases absurdes**
- Exemples : "Je voir ça si ça. Alice du paté.", "Je paté du pour."
- JSON malformé : "Unterminated string at position 85274"
- **Solution :** Retrait fallback Groq (utilisateur refuse TOUT fallback)

**3. Gemini 404 - modèles introuvables**
- Erreur : `models/gemini-1.5-flash is not found for API version v1beta`
- Testé : v1beta/gemini-1.5-flash ❌, v1/gemini-1.5-flash ❌, v1beta/gemini-pro ❌
- **Cause probable :** Quota API dépassé ou clé sans accès aux modèles
- **Solution appliquée :** Migration vers SDK GoogleGenerativeAI (comme generer-phrase.js)

**4. Utilisateur REFUSE les fallbacks**
- Citation : "je ne veux pas de ces merdes de fall back!"
- **Règle stricte :** Pas de génération dégradée, message d'erreur clair
- **Solution :** Message "Service temporairement indisponible. Veuillez réessayer plus tard."

### Code important

**Génération avec Gemini (SDK) - ACTUEL :**
```javascript
// pages/api/phrases/generer.js lignes 109-144
try {
    console.log('🤖 Génération avec Gemini...')

    const model = genAI.getGenerativeModel({ model: 'gemini-pro' })
    const result = await model.generateContent(prompt)
    const response = result.response
    const text = response.text()

    const cleanedText = text.replace(/```json\n?/g, '').replace(/```\n?/g, '').trim()
    const parsed = JSON.parse(cleanedText)

    if (parsed.phrases && Array.isArray(parsed.phrases)) {
        const motsAutorisesNormalises = motsUniques.map(normalizeWord)

        phrases = parsed.phrases.filter(p => {
            if (!p.texte || !p.mots || !Array.isArray(p.mots)) return false
            if (p.mots.length < 3 || p.mots.length > 7) return false
            return validatePhrase(p, motsAutorisesNormalises)
        })

        source = 'gemini'
    }
} catch (geminiError) {
    throw new Error('Service de génération de phrases temporairement indisponible. Veuillez réessayer plus tard.')
}
```

**Nettoyage cache Groq :**
```javascript
// /api/admin/clean-groq-cache.js
const { data, error } = await supabaseAdmin
    .from('phrases_pregenerees')
    .delete()
    .eq('source', 'groq')
    .select()
// Résultat : 290 phrases supprimées
```

### État actuel du problème Gemini

**Symptôme :** TOUS les modèles Gemini retournent 404
- `gemini-1.5-flash` (v1beta) → 404
- `gemini-1.5-flash` (v1) → 404
- `gemini-pro` (v1beta) → 404

**Preuve que ce n'est PAS un problème de code :**
- `/api/exercises/generer-phrase.js` utilise EXACTEMENT le même code
- Il échoue aussi avec Gemini → utilise son fallback
- La clé API `GOOGLE_AI_API_KEY` est la même

**Diagnostic probable :**
1. **Quota API Google dépassé** (le plus probable)
2. **Clé API sans accès aux modèles** (région, permissions)
3. **Modèles dépréciés** (peu probable pour gemini-pro)

**Action requise utilisateur :**
- Vérifier quota sur [Google AI Studio](https://makersuite.google.com/app/apikey)
- Vérifier modèles disponibles avec la clé
- Possiblement générer nouvelle clé API

### Notes pour la prochaine session

**IMPORTANT - Règles strictes utilisateur :**
1. ❌ **JAMAIS de fallback dégradé** (génération aléatoire, concaténation, etc.)
2. ✅ **Message d'erreur clair** si service indisponible
3. ✅ **Génération à la demande** (lazy loading) validée par utilisateur
4. ✅ **Cache en BDD** pour éviter regénération

**À faire quand Gemini sera résolu :**
- Tester génération phrases avec nouveau quota/clé
- Vérifier qualité phrases générées
- Vérifier cache fonctionne (2e appel rapide)

**Tables BDD :**
- `phrases_pregenerees` est le CACHE (pas "inutile" comme pensé initialement)
- AUCUNE autre table "phrase" ou "combinaison" n'existe
- Base de données propre, 45 tables toutes utilisées

**Fichiers à supprimer (temporaires) :**
- `clean-groq-cache.js` (racine projet) - Script one-shot exécuté

---

## [2025-11-15 00:15] - UI: Refonte page résultats Lis et trouve

### Fichier(s) modifié(s)
- `pages/lire/lis-et-trouve.js` - Refonte complète page résultats + fix recommencer

### Modifications

**1. Titre unifié**
- "🔊 Lis et trouve - Resultats" en turquoise #06B6D4
- Centré, responsive (20px mobile, 28px desktop)
- Suppression double affichage (lignes 1088-1096)

**2. Navigation 5 icônes**
- ← 👁️ 📖 🏠 🔄 (lignes 1099-1183)
- Icône recommencer (🔄) ajoutée après maison
- Responsive, identique mobile/desktop
- Flèche ← pointe vers `/lire/reconnaitre-les-mots/exercices2`

**3. Score turquoise**
- Bordure #06B6D4 au lieu de #3b82f6 (ligne 1188)
- Centré sous les icônes
- Responsive: 24px mobile, 32px desktop

**4. Titres listes de mots**
- "Mots réussis" sans ✅, turquoise, centré (lignes 1233-1237)
- "Mots à revoir" sans ❌, turquoise, centré (lignes 1284-1288)

**5. Suppression boutons redondants**
- Suppression section 3 boutons desktop (lignes 1322-1377 supprimées)
- Recommencer, Autres exercices, Menu Lire (doublons avec icônes)

**6. Fix bouton recommencer**
- Ligne 346: `setEtape('intro')` au lieu de `setGameFinished(false)`
- Retour correct à la page d'intro fonctionnel

### Code important
```javascript
// Fix restartGame - ligne 344
const restartGame = () => {
    setEtape('intro')  // ✅ Au lieu de setGameFinished(false)
    setScore(0)
    setAttempts(0)
    setCompletedMots([])
    setVisualFeedback({ clickedMotId: null, correctMotId: null, isCorrect: null })
    setFinalScore({ correct: 0, total: 0 })
    setResultats({ reussis: [], rates: [] })
}

// Navigation avec icône recommencer - lignes 1167-1182
<button onClick={restartGame}>🔄</button>
```

### Commit
- SHA: `631c378`
- Message: "UI: Refonte page résultats Lis et trouve"
- Lignes: +354 -356

---

## [2025-11-14 23:45] - UI: Nettoyage page résultats Écoute et trouve

### Fichier(s) modifié(s)
- `pages/lire/ecoute-et-trouve.js` - Suppression éléments redondants page résultats

### Modifications
- Titre en double supprimé: ajout condition `&& etape !== 'resultats'` ligne 836
- Emoji ✅ retiré du titre "Mots réussis" ligne 1467
- Emoji ❌ retiré du titre "Mots à revoir" ligne 1518
- Affichage uniquement "🎯 Écoute et trouve - Résultats" sur page résultats

### Commit
- SHA: `9c07f58`
- Message: "UI: Nettoyage page résultats Écoute et trouve"
- Lignes: +3 -3

---

## [2025-11-14 23:30] - Fix: Refonte complète exercice Écoute et trouve

### Fichier(s) modifié(s)
- `pages/lire/ecoute-et-trouve.js` - Multiples corrections interface, audio et logique

### Modifications

**1. Ajout consigne**
- Consigne turquoise "🔊 Écoute et clique sur le bon mot" avant grille de mots
- Design identique à "Où est-ce"
- Responsive mobile/desktop

**2. Fix système audio - Verrou isLoadingAudio**
- État `isLoadingAudio` ajouté pour empêcher appels multiples simultanés
- Verrou au début de `playAudio()` : ignore requêtes si chargement en cours
- Libération du verrou dans tous callbacks (onended, onerror, catch)
- **Problème résolu :** AbortError + sons en double

**3. Fix prononciation ElevenLabs/WebSpeech**
- Modification texte envoyé : `Le mot est : ${texte}` au lieu de `${texte}`
- Amélioration prononciation française pour voix anglaises ElevenLabs
- **Note :** Enregistrements personnels inchangés (juste le mot)

**4. Bouton "Suivant" si erreur**
- Passage auto 1.5s SEULEMENT si bonne réponse
- Si mauvaise réponse : bouton "Suivant" apparaît (fond blanc, bordure turquoise, texte turquoise + icône ➡️)
- Fonction `passerQuestionSuivante(wasLastCorrect)` refactorisée

**5. Page résultats - Refonte complète**
- **Titre :** "🎯 Écoute et trouve - Résultats" (une ligne)
- **Desktop :** Ajout 5 icônes manquantes (← 👁️ 📖 🏠 🔄)
- **Score :** Centré, couleur turquoise `#06b6d4` (au lieu de bleu)
- **Titres sections :** Centrés
- **Texte :** "Mots à revoir" au lieu de "Mots ratés"
- **Supprimé :** Boutons verts/bleus en bas (desktop)

**6. Fix confettis score parfait**
- Problème : `score` pas à jour (setState asynchrone)
- Solution : Paramètre `wasLastCorrect` dans `passerQuestionSuivante()`
- Calcul : `finalCorrect = wasLastCorrect ? resultats.reussis.length + 1 : resultats.reussis.length`
- **Confettis se déclenchent maintenant correctement**

**7. Fix boutons non cliquables**
- **Problème critique :** `completedMots` marquait mots comme "complétés" → disabled si réapparition
- **Solution :** Suppression complète logique `completedMots`
- Nouvelle condition disabled : `visualFeedback.isCorrect !== null || isPlaying`
- **Tous les mots cliquables à chaque question** ✅

### Code important

```javascript
// Verrou audio
const [isLoadingAudio, setIsLoadingAudio] = useState(false)

const playAudio = async (texte) => {
    if (isLoadingAudio) {
        console.log('⏸️ Audio déjà en cours de chargement, requête ignorée')
        return
    }
    setIsLoadingAudio(true)
    // ... reste du code
}

// Fix confettis
const passerQuestionSuivante = (wasLastCorrect = false) => {
    const finalCorrect = wasLastCorrect ? resultats.reussis.length + 1 : resultats.reussis.length
    setFinalScore({ correct: finalCorrect, total: finalTotal })
}

// Appel
setTimeout(() => passerQuestionSuivante(true), 1500)

// Fix boutons
disabled={visualFeedback.isCorrect !== null || isPlaying}
```

### Commit
- SHA: `707baea`
- Message: "Fix: Refonte complète exercice Écoute et trouve"
- Lignes: +251 -115

---

## [2025-11-14 21:30] - UI: Optimisation page résultats Découpage mobile

### Fichier(s) modifié(s)
- `pages/lire/reconnaitre-les-mots/decoupage-exercice.js` - Optimisation espace vertical mobile page résultats

### Modifications

**1. Titre optimisé pour mobile**
- Taille réduite: 24px (au lieu de 28px par défaut)
- `whiteSpace: 'nowrap'` pour empêcher retour à la ligne
- Desktop conservé à 36px

**2. Icônes navigation ajoutées en mobile**
- Suppression de la condition `!isMobile`
- 5 boutons visibles: ← 👁️ 📖 🏠 🔄
- Padding réduit: `6px 10px` (au lieu de `8px 12px`)
- Gap réduit: `6px` (au lieu de `8px`)
- `flexWrap: 'wrap'` pour adaptation écran

**3. Score optimisé pour mobile**
- Taille réduite: 28px (au lieu de 36px)
- Padding réduit: `12px 20px` (au lieu de `16px 32px`)

**4. Espacements drastiquement réduits (mobile)**
- `paddingTop` resultatsBox: **32px → 8px** (÷4)
- `marginBottom` score: **32px → 8px** (÷4)
- **Gain total: 48px d'espace vertical** (64px → 16px)

### Code important

**Titre adaptatif mobile:**
```javascript
<h1 style={{
    ...styles.title,
    fontSize: isMobile ? '24px' : '36px',
    whiteSpace: 'nowrap'
}}>✂️ Découpage - Résultats</h1>
```

**Icônes navigation (desktop ET mobile):**
```javascript
<div style={{
    display: 'flex',
    gap: isMobile ? '6px' : '8px',
    justifyContent: 'center',
    marginTop: '16px',
    flexWrap: 'wrap'
}}>
    {/* 5 boutons avec padding adaptatif */}
</div>
```

**Espacement résultats optimisé:**
```javascript
<div style={{
    ...styles.resultatsBox,
    paddingTop: isMobile ? '8px' : '32px'  // Réduction drastique
}}>
    <div style={{
        marginBottom: isMobile ? '8px' : '32px'  // Score collé aux icônes
    }}>
        <h2 style={{
            fontSize: isMobile ? '28px' : '36px',  // Score réduit
            // ...
        }}>
```

### Décisions architecturales
- **Optimisation mobile-first**: maximiser espace vertical pour listes phrases
- **Cohérence visuelle préservée**: couleurs turquoise et structure maintenues
- **Desktop inchangé**: toutes optimisations conditionnelles à `isMobile`
- **Gain d'espace significatif**: 48px libérés pour le contenu principal

### Pièges évités
- Ne pas oublier le `paddingTop` du conteneur parent (annulait l'effet du `marginBottom`)
- Utiliser `flexWrap: 'wrap'` pour éviter débordement horizontal
- Tester réduction progressive (16px puis 8px) pour trouver le bon équilibre
- Préserver desktop complètement intact

### Commit
- SHA: `7a51c72`
- Message: "UI: Optimisation page résultats Découpage mobile"
- Lignes: +83 -43

---

## [2025-11-14 19:30] - UI: Refonte complète exercice Découpage - Cohérence turquoise

### Fichier(s) modifié(s)
- `pages/lire/reconnaitre-les-mots/decoupage-exercice.js` - Refonte visuelle complète turquoise + UX desktop/mobile + page résultats

### Modifications

**1. Cohérence visuelle turquoise (#06B6D4)**
- Titre "✂️ Découpage" en turquoise
- Consigne en turquoise (texte + bordure)
- Suppression des ":" dans la consigne
- Séparations (barres |) en turquoise
- Icône œil 👁️ entre flèche et livre (bordure turquoise)

**2. Texte de découpage**
- Lettres en **noir gras** (au lieu de turquoise)
- Barres de séparation restent en turquoise
- Cadre adapté au contenu (width: fit-content)

**3. Boutons refondus**
- Bouton "Vérifier" : fond blanc, bordure turquoise 3px, texte turquoise gras
- Bouton "Suivant" (erreur) : fond blanc, bordure turquoise 3px, texte turquoise gras
- Versions mobile ET desktop

**4. Feedback et validation**
- Délai passage automatique : 2s → 4s
- Cadre "Parfait !" adapté au texte
- Consigne masquée quand feedback affiché
- Cadre rouge si aucun découpage + validation incorrecte

**5. Affichage correction (erreur)**
- Cadre vert (#10b981) au lieu de turquoise
- Suppression du titre "Bonne segmentation :"
- Mots en **noir gras**
- Séparations en **turquoise** (barres |) au lieu de tirets
- Cadre adapté au texte

**6. Page résultats refonte complète**
- Icône œil 👁️ ajoutée (entre flèche et livre)
- Icône recommencer 🔄 ajoutée (après maison)
- Score en turquoise dans cadre blanc centré (3px bordure turquoise)
- Suppression du %
- Suppression coches ✅ et ❌
- Titres centrés : "Phrases réussies" / "Phrases à revoir"
- Phrases centrées
- Suppression boutons du bas (Recommencer + Menu exercices)
- Confettis animés sur score parfait : 50 confettis colorés qui tombent

**7. Navigation cohérente**
- Icône œil 👁️ : `/lire/reconnaitre-les-mots`
- Bordure turquoise pour cohérence visuelle

### Code important

**Confettis animés (page résultats) :**
```javascript
{showConfetti && (
    <>
        <style dangerouslySetInnerHTML={{
            __html: `
                @keyframes confettiFall {
                    0% {
                        transform: translateY(-50px) rotate(0deg);
                        opacity: 1;
                    }
                    100% {
                        transform: translateY(100vh) rotate(360deg);
                        opacity: 0;
                    }
                }
            `
        }} />
        <div style={{...}}>
            {[...Array(50)].map((_, i) => {
                const emojis = ['🎉', '🎊', '✨', '🌟', '⭐']
                // Animation aléatoire pour chaque confetti
            })}
        </div>
    </>
)}
```

**Cadre correction avec barres turquoise :**
```javascript
<div style={{
    backgroundColor: 'white',
    border: '3px solid #10b981', // Vert
    width: 'fit-content',
    margin: '20px auto'
}}>
    {decoupageValidation.motsAttendus.map((mot, index) => (
        <span key={index}>
            <span style={{ color: '#000' }}>{mot}</span>
            {index < motsAttendus.length - 1 && (
                <span style={{ color: '#06B6D4', margin: '0 8px', fontSize: '28px' }}> | </span>
            )}
        </span>
    ))}
</div>
```

**Masquer consigne quand feedback :**
```javascript
{!isMobile && !feedback && (
    <p style={styles.consigne}>Clique entre les lettres pour séparer les mots</p>
)}
```

### Décisions architecturales
- Couleur turquoise `#06B6D4` comme standard pour tous les exercices "Reconnaître les mots"
- Lettres en noir pour meilleure lisibilité
- Séparations en turquoise pour cohérence visuelle
- Cadre vert pour la correction (distinct du cadre d'exercice)
- Page résultats épurée avec navigation par icônes uniquement
- Confettis animés identiques aux autres exercices

### Pièges évités
- Ne pas mettre les lettres en turquoise (lisibilité)
- Adapter TOUS les cadres au texte (fit-content)
- Masquer la consigne pendant le feedback
- Gérer le cas "aucun découpage" avec cadre rouge
- Versions mobile ET desktop pour tous les boutons

### Commit
- SHA: `342c60b`
- Message: "UI: Refonte complète exercice Découpage - Cohérence turquoise"
- Lignes: +202 -87

---

## [2025-11-14 17:30] - UI: Refonte complète exercice "Qu'est-ce ?" - Cohérence turquoise

### Fichier(s) modifié(s)
- `pages/lire/reconnaitre-les-mots/quest-ce-exercice.js` - Refonte visuelle complète turquoise + UX mobile/desktop

### Modifications

**1. Cohérence visuelle turquoise (#06B6D4)**
- Titre "🔊 Qu'est-ce ?" en turquoise
- Bandeau consigne en turquoise (texte + bordure)
- Mots du groupe de sens en turquoise
- Cadres des boutons sons en turquoise
- Chiffres dans les boutons sons (🔊 1, 🔊 2...) en turquoise

**2. Navigation améliorée**
- Ajout bouton œil (👁️) pointant vers `/lire/reconnaitre-les-mots`
- Bordure turquoise pour cohérence visuelle
- Position : entre flèche retour et bouton 📖

**3. Bandeau consigne optimisé**
- "🔊 Écoute les sons et choisis le bon" remonté à mi-distance score/groupe
- Texte centré dans le cadre
- Largeur minimale 400px pour alignement avec icônes
- Suppression du texte "Le mot illuminé est :"

**4. Bouton Valider refondu**
- Fond blanc (au lieu de bleu)
- Bordure turquoise 3px
- Texte turquoise en gras
- Cohérence avec le thème de l'exercice

**5. Page résultats optimisée**
- Score dans cadre turquoise centré sous les icônes
- Police 36px en gras (doublée)
- Suppression du pourcentage
- Suppression du cadre blanc autour des listes de mots
- Titres "Mots réussis" / "Mots à revoir" centrés
- Bouton Recommencer (🔄) après bouton 🏠
- Confettis sur score parfait (50 confettis colorés, 3 secondes)

**6. Version mobile spécifique**
- Groupe de sens centré (au lieu d'aligné à gauche)
- Texte en turquoise #06B6D4
- `whiteSpace: 'normal'` pour éviter débordement d'écran
- Passage automatique après 2 secondes sur réponse correcte
- Pas de bouton "Suivant" si réponse correcte (mobile uniquement)
- Bouton "Suivant" affiché uniquement sur réponse incorrecte

**7. Version desktop (inchangée comme demandé)**
- Bouton "Suivant" toujours affiché (réponse correcte ou incorrecte)
- Contrôle manuel du passage à la question suivante

### Code important

**Passage automatique mobile (réponse correcte) :**
```javascript
// Dans verifierReponseQuestCe()
if (isMobile && correct) {
    setTimeout(() => {
        questionSuivanteQuestCe()
    }, 2000)
}
```

**Affichage conditionnel bouton Suivant :**
```javascript
// Afficher uniquement si Desktop OU Mobile avec erreur
(!isMobile || !reponseValidee.correct) && (
    <button onClick={questionSuivanteQuestCe} style={styles.primaryButton}>
        Suivant ➡️
    </button>
)
```

**Groupe de sens mobile centré et adaptatif :**
```javascript
style={{
    textAlign: 'center',      // Centré
    whiteSpace: 'normal',     // Retour à la ligne auto
    color: '#06B6D4',         // Turquoise
    lineHeight: '1.4'         // Espacement lignes
}}
```

### Décisions architecturales
- Couleur turquoise `#06B6D4` comme standard pour tous les exercices "Reconnaître les mots"
- Distinction UX mobile/desktop : mobile plus fluide avec passage automatique
- Score en évidence sur page résultats (grande taille, cadre coloré)
- Confettis réservés au score parfait pour renforcer la réussite

### Pièges évités
- Ne pas casser la version desktop lors des modifications mobile
- Vérifier `isMobile` ET `correct` pour la logique de passage automatique
- Gérer `whiteSpace: 'normal'` en mobile pour éviter débordement horizontal
- Utiliser `setTimeout` pour le passage automatique sans bloquer l'UI

### Commit
- SHA: `62c89e2`
- Message: "UI: Refonte complète exercice 'Qu'est-ce ?' - Cohérence turquoise"
- Lignes: +172 -44

---

## [2025-11-14 15:00] - UI: Refonte complète exercice "Où est-ce"

### Fichier(s) modifié(s)
- `pages/lire/ou-est-ce-exercice.js` - Refonte visuelle complète + navigation + UX

### Modifications

**1. Cohérence visuelle avec "Reconnaître les mots"**
- Remplacement de toutes les couleurs bleues (`#3b82f6`) par turquoise (`#06B6D4`)
- Titre "📍 Où est-ce ?" en turquoise
- Texte des mots en turquoise
- Bordures des boutons en turquoise

**2. Système de feedback visuel amélioré**
- Suppression des messages texte ("✅ Correct !", "❌ Non, c'était...")
- Réponse correcte: cadre vert gras (4px) autour du mot cliqué
- Réponse incorrecte: cadre vert autour du bon mot + cadre rouge autour du mauvais choix
- Feedback visuel immédiat et clair

**3. Navigation corrigée**
- Icône œil (👁️): `/lire/reconnaitre-les-mots-new` → `/lire/reconnaitre-les-mots`
- Flèche retour (←): paramètre dynamique → `/lire/reconnaitre-les-mots/exercices2?textes=1`

**4. UX Desktop améliorée**
- Réponse correcte: passage automatique après 1.5s (inchangé)
- Réponse incorrecte: bouton "Suivant →" apparaît (pas de passage auto)
- Permet à l'apprenant de prendre le temps d'observer son erreur
- Mobile: passage automatique conservé (bon et faux)

**5. Optimisations UI**
- Consigne simplifiée: "🔊 Écoute et clique sur le bon mot" (suppression "bien")
- Consigne avec largeur minimale (400px) alignée sur les boutons de navigation
- Texte de consigne en turquoise
- Bouton "Suivant": fond blanc, bordure et texte turquoise (cohérence visuelle)
- Page résultats: suppression des cadres turquoise autour du score

### Code important

**État feedback modifié:**
```javascript
// Stocke le mot cliqué au lieu d'un message
const [feedback, setFeedback] = useState(null) // { type: 'success' | 'error', motClique: '...' }
```

**Fonction passage question suivante:**
```javascript
function passerQuestionSuivante() {
    setFeedback(null)
    const nextIndex = indexQuestion + 1
    if (nextIndex >= tousLesMots.length) {
        setEtape('resultats')
        return
    }
    const question = tousLesMots[nextIndex]
    if (question && question.mot && question.motsDuGroupe) {
        setMotActuel(question.mot)
        setGroupeActuel({...question.groupe, motsDuGroupe: question.motsDuGroupe})
        setIndexQuestion(nextIndex)
    }
}
```

**Logique passage conditionnelle (desktop vs mobile):**
```javascript
// Passage automatique : si mobile OU si bonne réponse
// Sur desktop avec erreur : attendre clic sur bouton "Suivant"
if (isMobile || correct) {
    setTimeout(() => {
        passerQuestionSuivante()
    }, 1500)
}
```

**Styles conditionnels des boutons:**
```javascript
// Cadre vert gras pour le bon mot
...(feedback && mot.toLowerCase() === motActuel.toLowerCase() ? {
    border: '4px solid #22c55e',
    boxShadow: '0 0 0 2px #22c55e'
} : {}),
// Cadre rouge gras pour le mauvais choix
...(feedback && feedback.type === 'error' && mot.toLowerCase() === feedback.motClique.toLowerCase() ? {
    border: '4px solid #ef4444',
    boxShadow: '0 0 0 2px #ef4444'
} : {})
```

### Décisions architecturales
- Feedback visuel > Feedback textuel (moins de lecture, compréhension immédiate)
- Turquoise `#06B6D4` comme couleur principale (uniformité avec "Reconnaître les mots")
- Distinction UX desktop/mobile: desktop permet plus de temps pour analyser les erreurs
- Fonction `passerQuestionSuivante()` réutilisable (setTimeout et bouton "Suivant")

### Pièges évités
- Ne pas supprimer complètement le feedback: conservation de l'état pour désactiver les boutons
- Vérifier `isMobile` ET `correct` pour la logique de passage automatique
- Conserver les deux formes de cadres (vert + rouge) simultanément en cas d'erreur

### Commit
- SHA: `7a4721d49d4ae43e5bbd59944da76bcf76b639e5`
- Message: "UI: Refonte complète exercice 'Où est-ce'"
- Lignes: +153 -57

---

## Session 1

### [14:XX] - Initialisation session de développement

**Type d'intervention:** Setup

**Contexte:**
- Démarrage d'une nouvelle session de développement
- Projet: Application pédagogique ACLEF
- Application pour public en situation d'illettrisme et alphabétisation
- Branch actuelle: `refonte-reconnaitre-mots`

**État du dépôt:**
```
Current branch: refonte-reconnaitre-mots
Status: (clean)

Recent commits:
- 5f02b3a: docs: Mise à jour journal DEV - Remettre dans l'ordre
- 1f879c1: Fix: Cascade audio + Navigation + Styles boutons - Remettre dans l'ordre
- 1918018: UI: Karaoké - Refonte couleurs jaune/marron
- 2e5603e: DEV: Mise à jour journal - Ma voix mes mots UI
- b32c78e: UI: Ma voix mes mots - Optimisation interface desktop
```

**Technologies clés:**
- Next.js 15.4.6 (Turbopack)
- React 19.1.0
- Supabase (PostgreSQL + Auth)
- Hyphenopoly (syllabification)
- Google Generative AI (Gemini)

**Modules principaux:**
1. **LIRE** - Lecture & apprentissage syllabique
   - Création/gestion de textes
   - Segmentation syllabique
   - 11 jeux de syllabes
   - Système de paniers
   - Corrections centralisées

2. **QUIZ** - Système de quiz IA
3. **IMAGIERS** - Collections d'images
4. **FORMATION** - Suivi pédagogique
5. **CODE-ROUTE** - Vocabulaire & exercices

**Points d'attention:**
- Application critique pour public en situation d'illettrisme
- Système de corrections centralisées (appliquées à TOUS les apprenants)
- Pages critiques à ne pas modifier sans permission
- Méthode de travail stricte: 1 fichier à la fois, approbation avant modification

**Prochaines étapes:**
- En attente des instructions utilisateur pour la session
- Prêt à travailler selon la méthode définie (proposition → approbation → modification → test)

---

