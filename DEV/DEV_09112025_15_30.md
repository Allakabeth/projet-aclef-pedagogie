# DEV - Session du 09/11/2025 15:30

## Début de session

- Date : 09/11/2025
- Heure : 15:30
- Contexte : Nouvelle session de développement
- Projet : ACLEF Pédagogique

---

## Notes de session

---

## [2025-11-09 22:25] - 💾 SAUVEGARDE: Résolution conflit MCP - Plugin vs .mcp.json

⚠️ **Sauvegarde avant compactage de conversation**

### Contexte
Travail sur configuration MCP pour le template New Project. Découverte d'un conflit entre le plugin supabase-toolkit et le fichier .mcp.json. Les serveurs du plugin prenaient la priorité et empêchaient le chargement des 7 serveurs configurés dans .mcp.json.

### Fichiers modifiés
- `/home/aclef/.claude/plugins/installed_plugins.json` - Suppression manuelle de l'entrée "supabase-toolkit@claude-code-templates"
- `/mnt/c/Projet ACLEF/PT3/.mcp.json` - Copié depuis template (7 serveurs configurés)
- `/mnt/c/Projet ACLEF/PT3/` - Installation complète du template via `/npinstall`

### Travail en cours
- ✅ Installation `/npinstall` dans PT3
- ✅ Copie configuration .claude/ (39 agents, 16 commandes)
- ✅ Copie fichiers templates (CLAUDE.md, Règles.md, .gitignore)
- ✅ Copie .mcp.json avec 7 serveurs
- ✅ Création structure DEV/ avec fichier horodaté
- ✅ Installation Context Monitor via npx
- ✅ Installation plugin supabase-toolkit (puis désinstallation)
- ✅ Identification du conflit plugin vs .mcp.json
- ✅ Suppression manuelle du plugin dans installed_plugins.json
- 🔄 Attente redémarrage Claude Code pour vérifier chargement des 7 serveurs

### Problème identifié
**Conflit MCP :** Le plugin supabase-toolkit installé GLOBALEMENT dans `/home/aclef/.claude/plugins/` chargeait ses 3 serveurs MCP et bloquait/masquait les 7 serveurs configurés dans `.mcp.json` du projet.

**Symptômes :**
- Commande `claude plugin uninstall supabase-toolkit` retournait "already uninstalled"
- MAIS le plugin était toujours présent dans `installed_plugins.json`
- DONC ses 3 serveurs MCP se chargeaient quand même
- Les 7 serveurs du `.mcp.json` n'étaient jamais chargés

**Solution appliquée :**
- Édition manuelle de `/home/aclef/.claude/plugins/installed_plugins.json`
- Suppression des lignes 4-11 (entrée supabase-toolkit)
- Redémarrage Claude Code requis pour vérifier

### Points clés à retenir
- **Plugin vs .mcp.json** : Les plugins installés globalement prennent la priorité sur .mcp.json
- **Bug désinstallation** : La commande `claude plugin uninstall` peut échouer silencieusement
- **Fichier de référence** : `/home/aclef/.claude/plugins/installed_plugins.json` contient la vraie liste
- **Commande /npinstall** : Ne devrait PAS installer le plugin supabase-toolkit, seulement copier .mcp.json
- **Format chemin WSL** : Toujours utiliser `/mnt/c/...` et non `C:\...`

### Serveurs MCP attendus (après redémarrage)
1. **supabase** - Toolkit Supabase (projet: mkbchdhbgdynxwfhpxbw)
2. **github** - Intégration GitHub
3. **sequential-thinking** - Raisonnement séquentiel
4. **deepwiki** - Documentation technique (HTTP)
5. **serena** - Assistant de recherche (uvx)
6. **memory** - Mémoire de session
7. **memory-bank** - Mémoire persistante (`/mnt/c/Projet ACLEF/.memory-bank`)

### Configuration .mcp.json
```json
{
  "mcpServers": {
    "supabase": {
      "command": "npx",
      "args": ["-y", "@supabase/mcp-server-supabase@latest", "--project-ref=mkbchdhbgdynxwfhpxbw"],
      "env": {"SUPABASE_ACCESS_TOKEN": "sbp_..."}
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {"GITHUB_PERSONAL_ACCESS_TOKEN": "github_pat_..."}
    },
    "sequential-thinking": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-sequential-thinking"]
    },
    "deepwiki": {
      "url": "https://mcp.deepwiki.com/mcp",
      "transport": "http"
    },
    "serena": {
      "command": "uvx",
      "args": ["--from", "git+https://github.com/oraios/serena", "serena", "start-mcp-server"]
    },
    "memory": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory"]
    },
    "memory-bank": {
      "command": "npx",
      "args": ["-y", "@allpepper/memory-bank-mcp@latest"],
      "env": {"MEMORY_BANK_ROOT": "/mnt/c/Projet ACLEF/.memory-bank"}
    }
  }
}
```

### Notes pour la prochaine session
- **Action immédiate** : Redémarrer Claude Code dans "New Project" et vérifier que les 7 serveurs se chargent
- **Si succès** : Mettre à jour `/npinstall` pour NE PAS installer le plugin supabase-toolkit
- **Si échec** : Envisager configuration manuelle des serveurs MCP via Settings UI
- **Alternative** : Utiliser uniquement .mcp.json et supprimer toute référence au plugin
- **Documentation** : Le fichier .mcp.json du projet EST vu par Claude Code mais peut être masqué par les plugins globaux

### Workflow utilisé
1. `/regles` - Rappel des règles
2. `/npinstall /mnt/c/Projet ACLEF/PT3` - Installation template
3. Détection problème MCP (seulement 3 serveurs au lieu de 7)
4. Investigation : lecture installed_plugins.json
5. Édition manuelle pour supprimer le plugin
6. Sauvegarde DEV.md avant test final

---

## [2025-11-09 18:55] - 💾 SAUVEGARDE: Installation template New Project + Configuration MCP & Context Monitor

⚠️ **Sauvegarde de routine après travail terminé**

### Contexte
Session complète d'installation et configuration du template New Project. Installation test réussie dans "Projet test" et mise à jour du template GitHub avec nouvelles fonctionnalités.

### Fichiers modifiés

**Template New Project (GitHub):**
- `.mcp.json` - Ajout configuration 7 serveurs MCP (Supabase, GitHub, Sequential-thinking, DeepWiki, Serena, Memory, Memory-Bank)
- `templates/.gitignore.template` - Ajout exclusion `.mcp.json` (protection tokens sensibles)
- `.claude/scripts/context-monitor.py` - Script Python de monitoring contexte en temps réel
- `.claude/settings.local.json` - Configuration statusLine pour context-monitor (local, non commité)

**Projet test (/mnt/c/Projet ACLEF/Projet test):**
- Structure complète installée via `/npinstall`
- Configuration `.claude/` (39 agents, 16 commandes)
- Fichiers `CLAUDE.md`, `Règles.md`, `.gitignore`
- Structure `DEV/` avec journal horodaté
- Configuration `.mcp.json` avec serveurs MCP
- Script `context-monitor.py` installé

### Travail en cours
- ✅ Exécution `/npinstall` pour "Projet test"
- ✅ Détection absence `.mcp.json` dans template
- ✅ Copie `.mcp.json` depuis projet pédagogie vers template
- ✅ Mise à jour `.gitignore` pour exclure `.mcp.json`
- ✅ Installation `statusline/context-monitor` dans template ET Projet test
- ✅ Commit et push vers GitHub (2 commits)
- ✅ Sauvegarde dans DEV.md

### Points clés à retenir
- **Commande `/npinstall`** : Installe uniquement la configuration Claude (pas l'architecture projet)
- **Format chemin WSL** : Utiliser `/mnt/c/...` et non `C:\...`
- **Configuration MCP** : 7 serveurs installés (tokens sensibles protégés par gitignore)
- **Context Monitor** : Affiche barres progression, alertes colorées, analytics session dans statusline
- **Pattern utilisé** : Template réutilisable pour standardiser configuration sur tous les projets
- **Sécurité** : `.mcp.json` exclu du git via `.gitignore` (contient tokens Supabase et GitHub)

### Serveurs MCP configurés
1. **supabase** - Toolkit Supabase (projet: mkbchdhbgdynxwfhpxbw)
2. **github** - Intégration GitHub
3. **sequential-thinking** - Raisonnement séquentiel
4. **deepwiki** - Documentation technique
5. **serena** - Assistant de recherche
6. **memory** - Mémoire de session
7. **memory-bank** - Mémoire persistante (`/mnt/c/Projet ACLEF/.memory-bank`)

### Commits GitHub
- **Commit `9e6541f`** : Feature: Ajout configuration MCP serveurs dans template
- **Commit `ee48312`** : Feature: Ajout context-monitor pour suivi utilisation tokens
- **Dépôt** : https://github.com/Allakabeth/New-project.git
- **Branche** : main

### Code/Configuration importante

**Configuration statusLine (settings.local.json):**
```json
{
  "statusLine": {
    "type": "command",
    "command": "python3 .claude/scripts/context-monitor.py"
  }
}
```

**Exclusion gitignore:**
```gitignore
# MCP Configuration (contains sensitive tokens)
.mcp.json
```

### Notes pour la prochaine session
- Template New Project maintenant complet avec MCP + Context Monitor
- Tous les futurs projets créés avec `/npinstall` auront automatiquement :
  - Configuration MCP (7 serveurs)
  - Context Monitor dans statusline
  - Protection des tokens sensibles
- Prochaine étape : Tester `/npinit` pour générer architecture projet via questionnaire
- Journal DEV horodaté fonctionne parfaitement

### Workflow utilisé
1. `/regles` - Rappel des règles de travail
2. `/npinstall` - Installation template dans nouveau projet
3. Détection problème → Question → Validation → Exécution
4. Installation MCP (copie + gitignore)
5. Installation context-monitor (npx claude-code-templates)
6. `/push` - Commit + Push GitHub
7. `/save` - Sauvegarde DEV.md

---

## [2025-11-09 15:35] - 💾 SAUVEGARDE: Session de configuration initiale

⚠️ **Sauvegarde de routine - aucun travail en cours**

### Contexte
Session très courte - uniquement push de la réorganisation documentation. Aucune modification de code.

### Fichiers modifiés
- Aucun fichier de code modifié dans cette session

### Travail en cours
- ✅ Push de la réorganisation documentation (commit `a472197`)
- ✅ Mise à jour journal DEV
- ✅ Vérification commandes /push et /save

### Points clés à retenir
- **Commande /push** : Fonctionne correctement pour git commit + push + mise à jour DEV.md
- **Commande /save** : Testée, permet sauvegarde sans commit
- **Nouveau workflow** : Fichiers DEV horodatés créés automatiquement au début de chaque session

### Notes pour la prochaine session
- Session de configuration terminée
- Prêt pour du vrai travail de développement
- Tous les rituels en place (DEV.md, règles, commandes)

---

## [2025-11-09 15:30] - Docs: Réorganisation structure documentation

### Fichiers modifiés
- `CLAUDE2.md` - Supprimé (obsolète)
- `DEV.md` - Supprimé (remplacé par structure horodatée)
- `DEV/DEV_09112025_15_30.md` - Créé (nouveau journal horodaté)
- `DEV/DEV.md` - Créé (archive des anciennes entrées)
- `CLAUDE.md` - Mis à jour (clarifications mineures)

### Points clés à retenir
- **Structure DEV/** : Nouveau système de journaux horodatés pour suivre les sessions
- **Pattern utilisé** : Un fichier par session (format `DEV_JJMMAAAA_HH_MM.md`)
- **Objectif** : Meilleure traçabilité chronologique des modifications

### Commit
- SHA: `a472197`
- Message: "Docs: Réorganisation structure documentation"
- Poussé vers: `origin/main`

---
