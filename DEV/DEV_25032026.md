# DEV - 25/03/2026

## [2026-03-25 10:26] - Fix: Jeux syllabes - réponses interchangeables quand parties visibles identiques

### Fichier(s) modifié(s)
- `pages/lire/syllabes-jeu-2.js` - Jeu 5 (Fin) : accepter les fins interchangeables quand le début affiché est le même
- `pages/lire/syllabes-jeu-1.js` - Jeu 4 (Début) : accepter les débuts interchangeables quand la fin affichée est la même

### Modifications
- Problème : si "agent" et "avant" sont dans le jeu, les deux affichent "a_" → impossible de savoir où mettre "gent" ou "vant"
- Solution : quand on dépose une syllabe sur un mot et qu'un autre mot non complété a la même partie visible, on accepte la réponse et on permute les attentes
- Même logique appliquée aux deux jeux (début et fin)

### Code important
```javascript
// Si pas correct directement, vérifier si un autre mot non complété
// a le même début et cette syllabe comme fin
let swapWordIndex = -1
if (!isCorrect) {
    swapWordIndex = newGameWords.findIndex((w, i) =>
        i !== wordIndex &&
        !w.isCompleted &&
        w.firstSyllables === word.firstSyllables &&
        w.lastSyllable === draggedSyllable
    )
    if (swapWordIndex !== -1) {
        isCorrect = true
    }
}
// Si échange nécessaire, permuter les fins attendues
if (swapWordIndex !== -1) {
    newGameWords[swapWordIndex].lastSyllable = word.lastSyllable
    word.lastSyllable = draggedSyllable
}
```

### Commit
- SHA: `9a5f476` (main) / `726be60` (ortho)
- Message: "Fix: Jeux syllabes - accepter les réponses interchangeables quand les parties visibles sont identiques"
- Lignes: +52 -4
